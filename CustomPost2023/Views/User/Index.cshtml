@using CustomPost2023.Data.Models
@{
    ViewBag.Title = "Список пользователей";
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}
<div class="content">
    <h2>Список пользователей</h2>
    <form method="get">
            <div>
                <label>Имя: </label>
                <input type="text" placeholder="Введите имя" name="nameStr">
                <label>Логин: </label>
                <input type="text" placeholder="Введите логин" name="loginStr">
                <label>Пароль: </label>
                <input type="text" placeholder="Введите пароль" name="passStr">
                <label>Роль: </label>
                <input type="text" placeholder="Введите роль" name="roleStr">

                
                <input type="submit" value="Фильтр" />
            </div>
    </form>
    <p><a asp-action="Create">Добавить пользователя</a></p>
    <table class="table">
        <tr><th>ID</th><th>Имя</th><th>Login</th><th>Password</th><th>Role</th><th></th><th></th></tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.st.user_id</td>
                <td>@item.st.user_name</td>
                <td>@item.st.login</td>
                <td>@item.st.password</td>
                <td>@item.st.role</td>
                <td>
                    <a asp-action="Edit" asp-controller="User" asp-route-id="@item.st.user_id">Изменить</a>
                </td>
                <td>
                    <form asp-controller="User" asp-action="Delete" method="post" asp-route-id="@item.st.user_id">
                        <input type="submit" value="Удалить" />
                    </form>
                </td>
            </tr>
        }
    </table>
</div>