@using CustomPost2023.Data.Models

@{
    ViewBag.Title = "Список типов транспорта доставки";
    Layout = "/Views/Shared/_AdminLayout.cshtml";
}
<div class="content">
    <h2>Список типов транспорта доставки</h2>
    <p><a asp-action="Create">Добавить тип</a></p>
    <table class="table">
        <tr>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.IdSort">
                    ID
                </a>
            </th>
            <th>
                <a asp-action="Index" asp-route-sortOrder="@ViewBag.nameSort">
                    Имя
                </a>
            </th>
            <th></th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.st.vehicle_type_id</td>
                <td>@item.st.vehicle_type_title</td>
                <td>
                    <a asp-action="Edit" asp-controller="VehicleType" asp-route-id="@item.st.vehicle_type_id">Изменить</a>
                </td>
                <td>
                    <form asp-controller="VehicleType" asp-action="Delete" method="post" asp-route-id="@item.st.vehicle_type_id">
                        <input type="submit" value="Удалить" />
                    </form>
                </td>
            </tr>
        }
    </table>
</div>